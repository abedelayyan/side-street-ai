{
  "name": "@side-street/session-do",
  "version": "0.0.0",
  "description": "Cloudflare Worker + Durable Object binding the session actor to SQLite and hibernating WebSockets",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "benchmark": "vitest run --config vitest.benchmark.config.ts",
    "lint": "eslint src test benchmark",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@side-street/core": "workspace:*",
    "@side-street/redaction": "workspace:*",
    "@side-street/session": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20250705.0",
    "@types/node": "^24.0.0",
    "vitest": "^3.2.0",
    "wrangler": "^4.24.0"
  }
}
