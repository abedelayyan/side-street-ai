{
  "name": "@side-street/web",
  "version": "0.0.0",
  "description": "Web viewer and steering UI for Side Street sessions",
  "license": "AGPL-3.0-only",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src test",
    "dev": "vite"
  },
  "dependencies": {
    "@side-street/core": "workspace:*",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
